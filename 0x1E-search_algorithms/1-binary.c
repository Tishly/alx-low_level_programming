#include <stdio.h>
#include <stdlib.h>

/**
 * binary_search - search for value in sorted arrays
 * @array: pointer to the first element
 * @size: size of the array
 * @value: element to search for
 *
 * Rrturn: -1 if array is null or value absent, else return value
 */

int binary_search(int *array, size_t size, int value)
{
	unsigned int i;
}
